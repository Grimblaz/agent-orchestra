<!-- markdownlint-disable-file MD013 -->

# Cross-tool Handoff Markers

Durable GitHub comment markers used by Agent Orchestra for phase handoffs, smart resume, engagement records, and credit ledger. Markers are HTML comments written to GitHub issue or PR threads; because they live on the issue, work resumes across sessions without losing context.

Row-level survival and fallback semantics: [../SKILL.md](../SKILL.md) (session-memory-contract).  
Persistence rationale: [../../../Documents/Design/session-memory-contract.md](../../../Documents/Design/session-memory-contract.md).

## Active Marker Families

**Write path (issue #893)**: every family below marked **[persist-marker]** is written exclusively via `skills/session-memory-contract/scripts/persist-marker.ps1` — the ONLY documented write path for that family, mirroring `persist-phase-ledger.ps1`'s established "never by hand-authoring" language; never a hand-composed `gh issue comment` / `gh pr comment` call. Families marked **[hand-authored]** are not yet migrated (either explicitly out of registry v1 scope, or a known v1 registry gap) and stay on their existing write path.

- `<!-- experience-owner-complete-{ID} -->` **[persist-marker]** (family `experience-owner-complete`, `post-new`) — upstream framing complete
- `<!-- design-phase-complete-{ID} -->` **[persist-marker]** (family `design-phase-complete`, `post-new`) — technical design complete
- `<!-- engagement-record-experience-{ID} -->` **[persist-marker]** (family `engagement-record`, `post-new`) — durable engagement audit for /experience phase: load-bearing decisions, audit rationale, articulation text persisted alongside the experience-owner-complete marker for cross-session decision memory (SMC-20)
- `<!-- engagement-record-design-{ID} -->` **[persist-marker]** (family `engagement-record`, `post-new`) — durable engagement audit for /design phase: load-bearing decisions persisted alongside the design-phase-complete marker; consumed by solution-authoring's same-decision-resume rule on phase re-entry (SMC-20)
- `<!-- engagement-record-plan-{ID} -->` **[persist-marker]** (family `engagement-record`, `post-new`) — durable engagement audit for /plan phase: load-bearing decisions persisted alongside the plan-issue marker; consumed by solution-authoring's same-decision-resume rule on phase re-entry (SMC-20)
- `<!-- engagement-record-orchestration-{ID} -->` **[persist-marker]** (family `engagement-record`, `post-new`) — durable engagement audit for orchestration touchpoint (`scope-classification`): persisted as an issue comment when scope-classification resolves; payload Markdown mirror co-located in the comment; consumed by solution-authoring's same-decision-resume rule on Code-Conductor re-entry (SMC-20)
- `<!-- engagement-record-review-{PR} -->` **[hand-authored — known v1 registry gap]** — durable engagement audit for /orchestra:review and /orchestra:review-judge phases: load-bearing review-finding dispositions persisted as a PR comment after the post-judge disposition gate completes; consumed by same-decision-resume on re-review of the same PR (SMC-20, SMC-23, schema_version 4). The `engagement-record` registry row declares `TargetSurface: issue` for every phase, but `review` is PR-keyed — a `review`-phase write through `persist-marker.ps1` is refused by the surface preflight until this gap is closed; stays hand-authored via `gh pr comment` in the interim.
- `<!-- review-dispositions-{PR} -->` **[persist-marker]** (family `review-dispositions`, `post-new`, `review-dispositions-validator-core.ps1` in-process validator adapter) — per-finding disposition record for PR code-review verdicts; covers all findings, routine and load-bearing (not judge-sustained findings only), one entry per finding carrying stable_finding_key, pass, disposition (incorporate|dismiss|escalate|defer), classification, and disposition_rationale (SMC-23)
- `<!-- open-for-work-affirmed-{ID} -->` **[persist-marker]** (family `open-for-work-affirmed`, `post-new`) — the open-for-work affirmation record (issue #957 D5, registered by #974): an issue comment carrying the what-statement the person affirmed at beat 1, which is what makes a brief's **authority source (b)** checkable rather than asserted. **Writer**: the open-for-work conversation (`skills/open-for-work/SKILL.md` § Writing the affirmation record), via `skills/session-memory-contract/scripts/persist-marker.ps1` — the ONLY documented write path for this family. **Reader**: any run resuming the issue, and `commands/plan.md`'s pre-flight. **`post-new` is load-bearing, not stylistic**: `Documents/Design/open-for-work.md` § The affirmation record voids an edited record as an ordering witness, and has the escape hatch post a *new* record rather than editing the old one — an in-place write shape destroys both. (The beat-2 re-route count is what the conversation observed, **not** an arithmetic over these records: record counts are a diagnostic cross-check that legitimately disagrees when a write no-ops, when a record is voided by a later edit, or when a retry appends twice.) **Interim practiced form**: records predating this registration are never written through this family and carry no marker at all — they are plain comments posted with `gh issue comment` whose first line is exactly `**Open-for-work affirmation (interim form, issue 957 Amendment 8) - issue {ID}**`, and they remain valid authority for their issue permanently (that document's § Supersession), so a resume scans for both forms. **Edit-state note**: `gh issue view --json comments` carries `includesCreatedEdit` but **no** `updated_at`, so the void-if-edited rule can only be evaluated from `gh api repos/{owner}/{repo}/issues/{ID}/comments`. **Survival semantics**: `durable` (GitHub issue comment on the subject issue).
- `<!-- design-issue-{ID} -->` **[hand-authored — legacy Copilot session-memory concept]** — durable design snapshot handoff used for D9 pause/resume and full-pipeline smart resume; not a live registry family (no active writer emits this literal on the current Claude pipeline; see `.github/copilot-instructions.md` for the frozen Copilot-only usage)
- `<!-- plan-issue-{ID} -->` **[persist-marker]** (family `plan-issue`, `upsert-in-place` + `plan-issue-write-back-preserve` post-step) — approved plan persisted
- `<!-- completion-account-{ID} -->` **[persist-marker]** (family `completion-account`, `upsert-in-place`) — the durable account a run leaves when it declares itself done (issue #949, registered by #998). **Issue-keyed, and that is the point rather than a default**: every review-pipeline artifact carrying finding-level content is keyed on a pull request, and a conductorless run reviews *before* one exists, so a PR-keyed account has nowhere to land at the moment it is written. The issue number is the one identifier that exists first. **Writer**: any run declaring itself done, via `skills/session-memory-contract/scripts/persist-marker.ps1` — the ONLY documented write path for this family. **Reader**: `Read-CompletionAccount` (`skills/verification-before-completion/scripts/completion-account-core.ps1`), and any later reader on any machine. **Payload**: format is the run's own choice except for one required assertion, `adversarial_review_ran: true` or `false` — two polarities, both lexically present, with **absence reading as not-run rather than as clean**. That false polarity is what stops a run that dispatched no review from writing a closed-looking account, since the "every finding traces to an outcome" property is vacuously true over an empty finding set. **`upsert-in-place` is deliberate**: an account is one standing statement about one run, revised as its fix cycle closes findings; `post-new` would leave a reader choosing among several accounts for the same issue with no rule saying which governs. Contrast `open-for-work-affirmed` above, whose append-only shape is load-bearing because each record is an ordering witness — an account witnesses no ordering. **What upsert costs, recorded rather than implied**: an account revised from `false` to `true` leaves no recoverable prior content, so in-place revision returns the last word to the author of a record whose whole point is that someone other than the author can adjudicate it. `gh api repos/{owner}/{repo}/issues/{ID}/comments` carries `includesCreatedEdit`, which is the only edit-state evidence available (see the `open-for-work-affirmed` row's Edit-state note); a reader that treats a revised account as authoritative should say that it was revised. **Planted-record exposure**: this family is recognised by shape alone, so anyone who can comment can post something that reads as a completion account, and the primitive's canonical match is the EARLIEST comment id — meaning a record planted before the run is both what a reader retrieves until the run writes, and the comment the run then PATCHes. The mitigation is the one this repository already settled on for shape-recognised issue-comment records (#957 amendments 11/13, shipped by #995): the reading surface **states who posted it**. `Get-CompletionAccountFromComments` carries the comment author through and reports both an undisclosed author and a multi-candidate collision. **`ValidatorAdapter` is `$null` deliberately, not by omission**: a validator adapter is a hard pre-write refusal (`skills/review-judgment/SKILL.md` § validator findings), which would make a non-conforming account *unwritable* rather than flagged and would build the fail-the-run detector #949 rejected. Both of this chunk's mechanisms are warn-only. **Survival semantics**: `durable` (GitHub issue comment on the subject issue).
- `<!-- frame-credit-ledger-{PR} -->` — warn-only frame credit-ledger comment posted by the pre-PR hook (sub-issue #429 of frame umbrella #425); idempotently upserted on every PR after `gh pr create`
- `<!-- review-judge-produced-{PR} -->` **[persist-marker]** (family `review-judge-produced`, `post-new`, `sentinel-empty` validator adapter) — sentinel written by the judge (both Copilot and Claude) immediately after the ruling finalizes, before pipeline-metrics persistence; the warn-only hook detects this to synthesize a `not-persisted` review credit when the PR body carries no review credit yet (SMC-16)
- `<!-- credit-input-{port}-{ID} -->` **[persist-marker]** (family `credit-input`, `post-new`, YAML-shape validator adapter) — deferred-emission marker written by pipeline-entry agents (Experience-Owner, Solution-Designer, Issue-Planner) immediately after their completion marker; payload is a `yaml` fenced block carrying `{ port, adapter, evidence }`; harvested by Code-Conductor at PR-creation time to emit the corresponding credit row (SMC-17)
- `<!-- proposed-followups-{PR|ISSUE} -->` — headless-queue fallback for the maintainer-approval filing gate (issue #837): posted as a PR or issue comment when no interactive surface is available, carrying proposed follow-up issues as a fenced YAML payload; transitions through `proposed` → `claimed` → `consumed` states. The `followup-` durable drop/modify records reuse the existing SMC-20 (issue-keyed) and SMC-23 (`engagement-record-review-{PR}`, PR-keyed) markers additively — this marker covers only the headless queue payload itself, not the drop/modify records (SMC-24)
- `<!-- frame-slices-{ID} -->` **[persist-marker]** (family `frame-slices`, `upsert-in-place` + `frame-slices-spine-splice` post-step) — sibling comment (issue #863) carrying every `<!-- frame-slice ... -->` block for the plan's implementation steps, plus a `frame-slices-generated-at` stamp that must equal the spine's `generated_at` value. **Writer**: Issue-Planner, at plan persist and re-persist, via `skills/session-memory-contract/scripts/persist-marker.ps1` — the ONLY documented write path for this family (`skills/plan-authoring/SKILL.md` § Spine and Slice Discipline). First persist creates the sibling; the family's registered `frame-slices-spine-splice` post-step then automatically writes the sibling's comment id back onto the plan comment's `frame-spine` block as `slice_comment_id` (863-D3) — refusing if the plan comment carries no `plan-issue-{ID}` marker or if `generated_at`/`frame-slices-generated-at` mismatch — so the agent no longer hand-splices this write-back. Re-persist reuses the existing sibling (found via its identity marker or the existing pointer) rather than creating a second one. **Reader**: Code-Conductor and Spine-Runner, via the `frame-spine-lookup` two-fetch-and-concatenate shim — the shim fetches the plan comment plus this sibling by the `slice_comment_id` Dispatch Input and concatenates the bodies before invoking `-Op Lookup`; the shim never parses the spine itself, and `frame-validate-core.ps1`'s structural checks (port-to-slice coverage) likewise expect the concatenated form when run manually against a split plan (`frame-validate -Mode plan` is manual-only; see `Documents/Design/phase-containment-ledger.md` for the exact error a bare, unconcatenated plan comment produces). `Invoke-FSCSpineLookupCli` cross-checks the sibling's `frame-slices-generated-at` stamp against the spine's `generated_at` and fails loud (`stale-spine`) on divergence, on pointer-present/marker-absent, and on a duplicate slice id across the concatenated corpus. **Survival semantics**: `durable` (GitHub issue comment on the subject issue); part of the plan-comment family the SMC-01 row governs, split out for reader-specific fetch scoping rather than a new persistence mechanism (no-new-mechanism rule). Legacy (pre-#863) plans carry no `slice_comment_id` and have no sibling — the lookup shim's single-fetch path stays the behavior for that population.
- `<!-- phase-containment-ledger-{ID} -->` **[persist-phase-ledger.ps1 — explicit non-goal of issue #893's registry v1]** — sibling comment (issue #863) carrying every `<!-- phase-containment-{ID} -->` block for the plan's sustained plan-stress-test findings, plus the plan-surface `<!-- judge-rulings ... -->` machine block — the two families co-move together (863-D4) so Fix A's co-location gate (`phase-containment-emission-check-core.ps1`, issue #782) stays satisfied: a `judge-rulings` head only authorizes counting the `phase-containment` blocks that share its own comment body. **Writer**: Issue-Planner, at Post-Judge Reconciliation (`skills/plan-authoring/SKILL.md` § Phase-containment emission, § Judge-rulings machine block). First persist creates the sibling — its body opens with the identity marker — and records its comment id back onto the plan comment as a standalone `<!-- phase-containment-ledger-ref: {comment_id} -->` marker placed immediately after the `<!-- plan-issue-{ID} -->` marker (863-D11); re-persist replaces the prior `judge-rulings` block on the sibling in place (never appends a second one) and appends any new `phase-containment` blocks. **Reader**: the phase-containment emission-check subsystem (`phase-containment-emission-check.ps1`, `Get-EmissionGap`, `Get-SustainedFindingCount`) and the rolling-history walker (`phase-containment-rolling-history-core.ps1`) — both fetch every comment on the issue via `gh api`/GraphQL regardless of which comment carries the blocks, so the split does not change either reader's fetch cost (see `Documents/Design/phase-containment-ledger.md` for why the once-projected 5.6x reader-waste figure was retired as unactionable). **Survival semantics**: `durable` (GitHub issue comment on the subject issue); part of the plan-comment family the SMC-01 row governs. Legacy (pre-#863) plans carry no `phase-containment-ledger-ref` pointer and no sibling — the 811-D1 fallback in `Test-EmissionMarkerPresent` continues to read the plan comment's own prose/`judge-rulings` head directly for that population. **Write-only, navigation-only (863 M2/M17 fix)**: `phase-containment-ledger-ref` exists to help a human or agent find the sibling; no reader may treat the pointer's mere presence as evidence that a real head or block exists anywhere — `Get-EmissionGap`'s 811-D1 suppression gate binds on the `phase-containment-ledger-{Id}` identity marker actually being present on the qualifying body, never on the pointer alone, and any future "optimization" that trusts the pointer without also checking the marker match reopens the exact false-suppression vector the M2 fix closed.

- `goal-contract` — block head (inside the `plan-issue-{ID}` comment; issue #872, design decisions 872-D2/872-D9) carrying the goal-contract plan-seat variant's machine-checkable YAML: verification targets, invariants, evidence obligations, `general_experience_standard`, halt conditions, and budget. **Writer**: Issue-Planner, at plan persist for a `plan-variant: goal-contract` plan, per `skills/plan-authoring/SKILL.md § Goal-contract plan variant`. **Reader**: `goal-contract-core.ps1` (`.github/scripts/lib/goal-contract-core.ps1`) on behalf of `frame-validate-core.ps1`'s structural branch and the future #873 verification harness. **Survival semantics**: `durable` (GitHub issue comment on the subject issue); part of the `plan-issue-{ID}` comment family the SMC-01 row governs — this head is not a separate comment, it lives inside the plan comment itself.
- `goal-halt-report-{ID}` — durable issue-comment head (issue #872, design decision 872-D9; schema and emit primitive landed in #874 plan step 1) for a goal-run harness halt report when a run stops early per one of the contract's `halt_conditions`. Payload schema: `skills/goal-run/schemas/goal-halt-report.schema.json` (closed schema; `schema_version`, `issue`, `halt_reason`, `target_ref`, `plan_remediation`, `evidence[]`, `recommended_next_owner`, `arm`, `stage`, `claim_provenance`, `budget_snapshot`). **Writer**: `Invoke-GoalRunHaltEmit` (`.github/scripts/lib/goal-run-halt-core.ps1`), which schema-validates the report object and refuses (throws) rather than posting an invalid one; every `evidence[]`/`plan_remediation` string is inert-rendered (marker-delimiter substrings stripped) before the comment body is built. This is the schema-validating emit primitive only — the harness loop's real halt call-sites (deciding which `halt_reason` wins when multiple conditions are true at once, and invoking this emitter on every halt path) are later #874 steps, not yet wired to any live harness run. **Reader**: not yet implemented. **Survival semantics**: `durable` (GitHub issue comment on the subject issue).
- `goal-run-class` — a `required_markers` entry named in the 872-D2 sample `<!-- goal-contract -->` payload but not yet backed by a writer or reader in this repo. **Writer**: reserved for the goal-run harness child (#874+), which owns this marker's schema and emission. This row registers the name now (rather than dropping it from the design's sample payload) so a reader of the sample is not left wondering what an unregistered `required_markers` entry means. **Survival semantics**: reserved — no comment or block exists yet; do not treat this row as evidence the marker is currently emitted anywhere.
- `goal-run-inflight-{Issue}` — durable issue-comment mutex marker for a live `/goal-run {issue}` launch attempt (issue #874, plan step 4, M8; adoption primitive and additional resolution sites added in #912 step 4). Payload is a Markdown bullet list (schema_version, issue, status, contract_hash, launched_at, an optional resolved_reason once withdrawn, and an optional adopted_by_session_id once adopted), never a fenced YAML block. `status` is a three-value enum: `unresolved | resolved | adopted` — `adopted` is a distinct value from `resolved`, so a body read can tell "adopted, run in progress under a new session" apart from "resolved, marker withdrawn/completed". **Writer**: `New-GoalRunInflightMarker` (`.github/scripts/lib/goal-run-stage-core.ps1`), which always POSTs a fresh comment (never upserts) — launch order is marker-first, then provision: the marker is posted BEFORE `New-GoalRunWorktree` is ever called, and a post failure aborts the launch with no worktree created. After posting, `Invoke-GoalRunMutexLaunch` re-fetches every live marker for the issue and tiebreaks via `Resolve-GoalRunInflightMutexOutcome`. **Live means `status: unresolved` OR `status: adopted`** — an adopted marker is a run still in progress under a different session, not a withdrawn one, so excluding it would let a second invocation see an in-progress run as absent and provision a duplicate worktree. The tiebreak outcome: the lowest (earliest) comment id proceeds to provisioning, every other live marker yields and is patched to `status: resolved` via `Set-GoalRunInflightMarkerResolved`. Mutex-yield is no longer the only resolution site: `Resolve-GoalRunInflightMarkerForResolution` (same file) re-fetches and identifies the winning live marker — the same `unresolved`-or-`adopted` liveness test the mutex reconcile applies, so a marker this session itself just adopted stays findable when that session later resolves it — for every other halt or completion point in the `/goal-run` stage machine — the pre-loop launch-pin halt, every chain-stage-boundary halt, the Stage 1 fix-cycle-cap halt, the `loop-interrupted` guard/re-validation halts, and Stage 5's `run-complete` completion path (`agents/Goal-Run.agent.md`) — each of which then calls `Set-GoalRunInflightMarkerResolved` with a reason naming that specific site. A held marker can also transition to `adopted` instead of `resolved`: `Set-GoalRunInflightMarkerAdopted` PATCHes the marker to `status: adopted` plus the adopting session's id, then re-fetches and verifies the mutation actually landed (`Reason` one of `adopted-and-verified | already-adopted | already-resolved | marker-status-unrecognized | patch-failed | verification-comment-not-found | verification-body-unchanged | verification-status-mismatch | reconfirm-comment-not-found | lost-to-concurrent-adopter`) — this is the mutation `Resolve-GoalRunInvocationAction`'s `adopt-and-resume` action performs before a dead-appearing or explicitly force-adopted run resumes. **When the adoption pre-check's own re-read succeeds**, the adoption admits only a freshly-read `unresolved` status: a marker the owning run already resolved refuses as `already-resolved`, and one whose status line is missing or unrecognized refuses as `marker-status-unrecognized`, rather than being PATCHed back to `adopted`. That qualifier is load-bearing and the guarantee does not extend past it: the pre-check gate is nested inside a successful comment read, and `Get-GoalRunIssueComments` is fail-open (`@()` on any transport or parse failure), so a transient `gh` failure skips the gate entirely and the PATCH lands on a marker whose live status was never observed — including a `resolved` one, whose `resolved_reason` it then destroys. The function still reports `Verified: $false` (`verification-comment-not-found`) in that case, so a caller that stops on `Verified: $false` is safe, but the mutation has already happened. Do not read this row as license to skip a caller-side guard. Tracked by [#928](https://github.com/Grimblaz/agent-orchestra/issues/928). **Reader**: `Get-GoalRunInflightMarkers` (mutex reconcile) and `Test-GoalRunInflightAppearsDead` (crash-atomicity: a marker with no recent heartbeat is a first-class detectable "appears dead" state that `/goal-run {issue}` reports instead of silently launching a duplicate run). On `status`, that deadness check short-circuits on `resolved` ONLY — an `adopted` marker falls through to the same purely elapsed-time staleness computation an `unresolved` one gets, because an adopted run can crash just like any other. One non-status short-circuit also precedes the elapsed-time computation: a heartbeat that is present but unparseable returns `AppearsDead: $false` / `heartbeat-unparseable-assumed-live` regardless of status, failing closed rather than casting. Whether a terminal outcome (halt report or PR) exists never suppresses the staleness verdict; it is surfaced separately as `TerminalOutcomePresent` so the invocation-action precedence can compose "stale" and "terminal outcome present" as two independent signals. **Survival semantics**: `durable` (GitHub issue comment on the subject issue); this row now supersedes the prior "reserved, zero matches" registration from #874 plan step 1.
- `goal-run-stage-{Issue}` — durable issue-comment head (issue #874, plan step 4) carrying the goal-run harness's own minimal top-level stage-machine vocabulary: `loop-launched | loop-released | chain-dispatched`. `pre-loop` is NOT in this set — it is a resume stage the reader can conclude, never a value the writer records, so a run sitting at `pre-loop` is represented by the ABSENCE of this marker rather than by a marker carrying that word. A single comment, upserted in place (never appended-to) — always reflects only the LATEST completed stage, not a full history. This marker was not defined by any earlier #874 step; step 4 owns its introduction. **Writer**: `Set-GoalRunStageMarker` (`.github/scripts/lib/goal-run-stage-core.ps1`), called by the `/goal-run {issue}` stage machine at each of the three recorded transition points; `Clear-GoalRunStageMarker` (same lib file, #912 step 5) is an additional writer — the operator `/goal-run {issue} restart` lever's capture-then-clear sequence (`Invoke-GoalRunRestart`, `agents/Goal-Run.agent.md` § Operator Restart) deletes this comment outright rather than upserting it, so the marker's lifecycle is upsert-while-a-run-progresses, delete-on-operator-restart — not upsert-only. **Reader**: `Get-GoalRunStageMarker`, consumed by `Resolve-GoalRunResumeStage` — the pure state-detection function `/goal-run {issue}` uses to resume at the first incomplete stage on re-invocation, alongside the goal-contract hash check, the `goal-run-inflight-{Issue}` marker, `goal-run-active.json`, and the typed run log. The resume vocabulary that reader RETURNS is deliberately wider than the three values written here — `blocked | complete | chain-dispatched | loop-interrupted | loop-launched | pre-loop` — and the mapping is not one-to-one: a recorded `loop-released` resumes at `chain-dispatched` (the loop already finished; only the chain dispatch is outstanding), and a recorded `loop-launched` resumes at `loop-interrupted` (the loop started but never recorded release, so it must be resumed in progress rather than relaunched from scratch). `loop-interrupted` is also what a run-log checkpoint alone concludes when no stage marker survived at all. **Survival semantics**: `durable` (GitHub issue comment on the subject issue).
- `goal-run-restart-report-{Issue}` — durable issue-comment head (#912 D6, step 5) recording the worktree path and branch name of a run the operator `/goal-run {issue} restart` lever cleared, posted BEFORE `Clear-GoalRunStageMarker`/`Clear-GoalRunActiveState` ever run so the operator can recover any committed work by hand even after the stage marker and active-state file disappear — the branch name has no other durable home, since `New-GoalRunWorktree` mints a fresh GUID-suffixed branch per launch that neither `goal-run-active.json` nor the inflight marker ever store. Always POSTs a new comment (never upserts), so a history of restarts survives across repeated invocations on the same issue, the same always-post rationale the `goal-run-inflight-{Issue}` mutex marker already relies on. **Writer**: `New-GoalRunRestartReportBody`/`Invoke-GoalRunRestart` (`.github/scripts/lib/goal-run-stage-core.ps1`). **Reader**: none automated — this is an operator-facing forensic record only; a human reads it by hand to recover the worktree path and branch name. **Survival semantics**: `durable` (GitHub issue comment on the subject issue).
- `goal-probe-findings-{ID}` — durable issue-comment head (issue #874, capability-probe plan step 4) carrying the goal-loop capability probe's recorded decisions: **arm-H enablement** (with the surface discriminator naming which headless surface was actually verified, plus the credential mechanism used), **token-arm status**, and **native-budget-flag usability**. Each decision is inconclusive-aware: it records either an affirmative finding or `inconclusive → 874-D5 default (token arm advisory, wall-clock enforcing)` when its controlling leg could not be resolved, so a blank is never mistaken for a negative. The comment also carries the probe's flagged-not-resolved open questions. **Writer**: Code-Conductor, at probe step 4, authored from the owner's raw leg observations (the keyboard legs are owner-executed; the conductor does not run them). **Reader**: the deferred #874 goal-run harness plan — its sole consumer — which locates the probe's conclusions deterministically by this head rather than re-deriving them from evidence-document prose. Backing evidence: [`Documents/Design/goal-loop-capability-probe.md`](../../../Documents/Design/goal-loop-capability-probe.md), which carries the per-leg `observed | documented | static | inferred` labels and the explicit gaps. **Survival semantics**: `durable` (GitHub issue comment on the subject issue).

## Writing about markers safely

**The hazard**: every marker family in this document is detected by a raw-text scan (`-match`, `[regex]::IsMatch`, `.Contains`) against a comment or PR body — not a semantic parse. Markdown code-span backticks do NOT neutralize these scans: a complete marker literal such as the live delimited form of `judge-rulings pr=5` still matches even inside backticks, because the regex scans raw text and backticks are not stripped before matching. Any prose that quotes a marker literal verbatim — explaining the convention, giving a worked example, or describing what a comment "carries" — is therefore live to the same reader that scans real comments, and can produce a false positive (a prose mention miscounted as a real block or head) or, via `Find-OrUpsertComment`'s `-like` substring matcher, cause an entirely wrong comment to be selected as the target for a write.

**Families affected**: every marker family whose reader does a raw-text scan is affected — this covers every family in the Active Marker Families list above, including `pipeline-metrics`, `cost-pattern-data`, `phase-containment`, `phase-containment-ledger`, `frame-spine`, `frame-slice`, `credit-input`, `engagement-record`, `judge-rulings`, `plan-issue`, `design-phase-complete`, and `experience-owner-complete`. Assume the same hazard applies to any new marker family introduced later unless its reader is proven to do a genuine semantic parse (e.g., a GraphQL comment-id lookup) rather than a raw-text scan.

**The remedy**: use `Format-InertMarkerLabel` (`.github/scripts/phase-containment-emission-check.ps1:148-152`) to render a marker mention in prose. It strips the `<!--`/`-->` HTML-comment delimiters before backtick-wrapping, which genuinely breaks the regex match because the delimiters the pattern anchors on are no longer present in the text at all.

**Worked example**: rendering the live `judge-rulings pr=5` marker for prose reads as `` `judge-rulings pr=5` `` — never with the `<!--`/`-->` delimiters left intact inside the backticks. The same discipline applies to every marker family listed above: write `` `phase-containment-ledger-ref: 123` ``, not the delimited form of that same literal.

This rule was first written for the plan-surface `judge-rulings` block (`skills/plan-authoring/SKILL.md` § Judge-rulings machine block, writer rule 5), which now cross-references this section rather than duplicating it.

## Retired Markers

- `<!-- code-review-complete-{PR} -->` — retired in issue #441 Step 11; Code-Conductor reads `credits[]` from the PR-body pipeline-metrics block instead
