# Platform — Claude Code

> Auto-mode boundary: see [CLAUDE.md § Auto-mode boundary](/CLAUDE.md#auto-mode-boundary). Auto-mode does not suppress engagement gates.

When `design-exploration` escalates a choice to the user, present the exploration-phase summary plus the concrete options the methodology recommends. How that is presented is the agent's call per turn — this file specifies no mechanism. The agent retains ownership of approval behavior.
