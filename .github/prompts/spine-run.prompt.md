---
agent: Spine-Runner
description: "Run Spine-Runner against an issue with a persisted v2 frame-spine plan"
argument-hint: "Issue number"
---

# /spine-run

Start the Spine-Runner walk for: {{input}}

Copilot resolves `agent: Spine-Runner` by filesystem discovery of `agents/Spine-Runner.agent.md`; there is no separate registration list for this prompt.
